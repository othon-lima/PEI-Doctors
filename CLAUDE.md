Read and follow all instructions in `.cursorrules` — it contains the project rules, conventions, and architecture documentation. Always consult it before making changes.

When you introduce significant architectural changes, new dependencies, or new patterns, update `.cursorrules` to reflect the new state.
